packages:
  - apps/*
  - packages/*

# Packages allowed to run install scripts (pnpm 11 blocks them by default).
allowBuilds:
  esbuild: true
  mongodb-memory-server: false
  puppeteer: true
  sharp: true
  unrs-resolver: false
